'use client'

// const Leaderboard = dynamic(() => import('stare/leaderboard/Leaderboard'), {ssr: false});

const Page = () => {
    return (
        <div>Leaderboard</div>
    );
}
export default Page
